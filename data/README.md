# Data Directory

This directory stores both real and generated datasets with ML-friendly structure.

- `raw/`: immutable source data.
- `interim/`: temporary outputs from transformation steps.
- `processed/`: validated outputs used by downstream workflows.
- `external/`: third-party/reference datasets.
- `ml/`: machine-learning-ready datasets, features, and splits.

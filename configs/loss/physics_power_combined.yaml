kind: physics
_target_: mpinv.cli._builders.build_physics_power_loss
log_ratio: false
log_eps: 1e-12
# Both auxiliaries active (Cartesian sweep over the two weights in the
# step1_combined.yaml multirun config).
coef_aux_weight: 0.1
rank_bin_weight: 0.1
rank_bin_n_bins: null
rank_bin_beta: 10.0
